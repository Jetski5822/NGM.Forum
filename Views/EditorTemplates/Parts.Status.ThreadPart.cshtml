@model NGM.Forum.Models.ThreadPart

<fieldset>
        <label for="@Html.FieldIdFor(m => m.IsSticky)" class="forcheckbox">@T("Is Sticky")</label>
        @Html.CheckBoxFor(m => m.IsSticky)
</fieldset>