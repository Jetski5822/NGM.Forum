@using NGM.Forum
@using NGM.Forum.Extensions
@using NGM.Forum.Models
@{
    var forumPart = (ForumPart)Model.ContentPart;
}
  
<div class="item-properties actions">
    <a href="@Url.ThreadCreate(forumPart)" class="create">@T("Create Thread")</a>
</div>