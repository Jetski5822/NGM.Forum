@{ Layout.Title = T("New Forum Root").ToString(); }

@T("A Forum Home is the base url for a set of forums.  The title forms the base URL and the body is used to add a description about the forums.")
@using (Html.BeginFormAntiForgeryPost()) {
    @Html.ValidationSummary()
    // Model is a Shape, calling Display() so that it is rendered using the most specific template for its Shape type
    @Display(Model)
}