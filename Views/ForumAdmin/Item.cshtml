@{
    Html.AddTitleParts(T("Manage Forum").ToString());
}
    @* Model is a Shape, calling Display() so that it is rendered using the most specific template for its Shape type *@
    @Display(Model)
